import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
    submit_button: {
        color: 'red',
        fontSize: 30,

        backgroundColor: 'white',
        textAlign: 'center',
        borderRadius: 8,

    },
})