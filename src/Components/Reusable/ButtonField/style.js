import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
    button1: {
        marginTop: 10,
        marginBottom: 15,
        // alignItems: 'center',
        // backgroundColor: 'white',
        borderRadius: 5

    },
    buttonText: {
        padding: 10,
        // color: '#fff',
        // color: 'red',
        // fontSize: 20,
        fontWeight: 'bold',
    },

})